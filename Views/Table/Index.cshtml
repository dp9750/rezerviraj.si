@model IEnumerable<rezerviraj.si.Models.Miza>

@{
    ViewData["Title"] = "Index";
}

<div class="container pt-5">

    <h1>Vse mize</h1>

    @foreach (var miza in Model)
    {
        @Html.Raw(miza);
    }
    
</div>